//
//  BankVO.m
//  TheStoreApp
//
//  Created by yangxd on 11-7-25.
//  Copyright 2011 vsc. All rights reserved.
//

#import "BankVO.h"


@implementation BankVO

@synthesize bankAccount;                         //商户号
@synthesize bankname;                            //支付渠道名称
@synthesize bankSerial;                          //开户行号
@synthesize bankType;                            //支持渠道类型
@synthesize createtime;                          //创建时间
@synthesize gateway;                             //网关id
@synthesize nid;                                 //银行id
@synthesize logo;                                //渠道logo的Url地址
@synthesize rankNo;                              //排序
@synthesize updatetime;                          //更新时间

-(void)dealloc {
	if (bankAccount != nil) {
		[bankAccount release];
	}
	if (bankname != nil) {
		[bankname release];
	}
	if(bankSerial!=nil){
		[bankSerial release];
	}
	if (bankType != nil) {
		[bankType release];
	}
	if (createtime != nil) {
		[createtime release];
	}
	if (gateway != nil) {
		[gateway release];
	}
	if (nid != nil) {
		[nid release];
	}
	if (logo != nil) {
		[logo release];
	}
	if (rankNo != nil) {
		[rankNo release];
	}
	if (updatetime != nil) {
		[updatetime release];
	}
	[super dealloc];
}
@end
